#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int Alphabet = 'a';

	while (Alphabet <= 'z')
	{
		if (Alphabet != e && Alphabet != q)
		{
			putchar(Alphabet);
		}

		++Alphabet;
		putchar ('\n');
	}
	return(0);
}
