#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ch;

		/*printing lowercase alphabets using ASCII*/

		for (ch = 97; ch <= 122; ch++)
		{
			putchar(ch);
		}
			putchar('\n');

		return (0);
}
